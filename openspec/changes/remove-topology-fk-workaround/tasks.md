## 1. Verify upstream PG fix is available

- [x] 1.1 Confirm upstream PostgreSQL commit `34a3078629` ("Fix RI fast-path crash under nested C-level SPI") is present on PG master via `git log` in a fresh shallow clone
- [x] 1.2 Read the commit message and verify the fix scopes `AfterTrigger` batch callbacks to the correct resource-owner level (not just a guard flip)
- [x] 1.3 Confirm the commit explicitly states "deferred constraints are unaffected by this bug", validating that the PostGIS workaround's mechanism (deferring) was correctly dodging the same root cause

## 2. Prepare reverted worktree

- [x] 2.1 Create git worktree `../postgis-fix-topology-crash` from branch `feature/fix-topology-crash-latest`
- [x] 2.2 `git revert --no-edit 51e7d6202 a23712f27` in the worktree (TODO comments, then the FK workaround itself)
- [x] 2.3 Verify `topology/sql/manage/CreateTopology.sql.in` has exactly two `DEFERRABLE INITIALLY DEFERRED` constraints (`next_left_edge_exists`, `next_right_edge_exists`) and five immediate FK constraints on `node`/`edge_data`
- [x] 2.4 Verify `topology/topology_after_upgrade.sql.in` no longer contains the 81-line workaround migration block

## 3. Rebuild postgis-build-env:latest

- [ ] 3.1 Run `docker build` with `POSTGRES_BRANCH=master GEOS_BRANCH=main GDAL_BRANCH=master PROJ_BRANCH=master SFCGAL_BRANCH=master` and `BUILD_THREADS=2` (reduced from the default of 4 to avoid OOM during SFCGAL compilation)
- [ ] 3.2 Tag result as `postgis/postgis-build-env:latest`
- [ ] 3.3 Inside the built image, verify `git log` in PG source shows commit `34a3078629` — OR — confirm `src/backend/commands/trigger.c` contains the `AfterTriggersQueryData.batch_callbacks` field (introduced by the fix)
- [ ] 3.4 File a separate tracking issue against `postgis-build-env` documenting the transient `codeload.github.com` DNS failure in the nlohmann/json step AND the OOM with default `BUILD_THREADS=4` during SFCGAL compilation (both observed during this verification)

## 4. Run topology regression against rebuilt container

- [ ] 4.1 Start a container from `postgis/postgis-build-env:latest` with the reverted worktree bind-mounted at `/src/postgis`
- [ ] 4.2 Inside the container, run `./autogen.sh && ./configure --with-raster --with-topology --with-sfcgal && make -j2`
- [ ] 4.3 Run `sudo make install` to install into the container's `/usr/local/pgsql`
- [ ] 4.4 Start PG and run `make check RUNTESTFLAGS="--extension --topology --verbose"`
- [ ] 4.5 Confirm zero `ri_FastPathTeardown` crashes and zero `rd_refcnt > 0` assertion failures in PG logs
- [ ] 4.6 Specifically verify the test cases identified in `f393f6017` ("Document thorough investigation") as the original reproducers pass cleanly
- [ ] 4.7 If any failure occurs, stop and diagnose; do not proceed to step 5

## 5. Re-enable latest CI job

- [ ] 5.1 In the worktree, remove the `continue-on-error: ${{ matrix.name == 'latest' }}` flag (or equivalent) introduced by commit `064cbe5c9` in `.github/workflows/ci.yml`
- [ ] 5.2 Update the accompanying diagnostic comment in `ci.yml` to reference upstream PG commit `34a3078629` as the upstream resolution
- [ ] 5.3 Commit as a separate commit on `feature/fix-topology-crash-latest` with message "Re-enable latest CI job after upstream PG RI fast-path fix"

## 6. Verify the topology-fk-constraints spec matches reality

- [ ] 6.1 After `make install`, connect to a test database, run `SELECT topology.CreateTopology('specverify', 4326);`
- [ ] 6.2 Query `pg_constraint` for the constraints on `specverify.node` and `specverify.edge_data`; verify the immediate-vs-deferred status matches `specs/topology-fk-constraints/spec.md` exactly
- [ ] 6.3 Execute the forward-reference edge-insertion scenario from the spec (edge A referencing edge B via `abs_next_left_edge`, A inserted before B, commit expected to succeed)
- [ ] 6.4 Execute the negative-path scenario: INSERT into `edge_data` with a non-existent `start_node`, verify `foreign_key_violation` is raised at the INSERT statement (not at commit)

## 7. Update documentation and commit messages

- [ ] 7.1 The two revert commits generated by `git revert` already reference the original commits; add a summary commit on top (or amend the later revert) noting: "Upstream PG commit 34a3078629 by Amit Langote resolves the ri_FastPathTeardown crash; this revert restores immediate FK constraints on topology node/edge_data, matching upstream PostGIS."
- [ ] 7.2 Update `IronGateLabs/postgis#12` and `IronGateLabs/postgres#1` tracking issues with the resolution (link to PG commit, link to the PostGIS revert PR)
- [ ] 7.3 Do NOT re-alter existing topology schemas on upgrade. Note this decision in `topology/topology_after_upgrade.sql.in` via a brief comment if warranted, referencing the design doc's D2 decision

## 8. Merge and archive

- [ ] 8.1 Open a PR from `feature/fix-topology-crash-latest` → `develop` with the revert commits + CI re-enable commit + this openspec change
- [ ] 8.2 Wait for CI (including the now-blocking `latest` job) to pass
- [ ] 8.3 Merge to `develop`
- [ ] 8.4 Archive this openspec change via `openspec archive remove-topology-fk-workaround`
- [ ] 8.5 Delete the `../postgis-fix-topology-crash` worktree after the merge

## 9. Parallel build-env fix (separate PR)

- [ ] 9.1 Open a PR from `IronGateLabs/postgis-build-env` branch `fix/add-docbook-xsl-for-garden` upstream to `postgis/postgis-build-env` — commit `b262fbe` adds `docbook-xsl-ns` to the Dockerfile for garden test support
- [ ] 9.2 This is orthogonal to the topology FK work but was discovered during the same verification session; track separately so the topology fix does not block on build-env upstream review
